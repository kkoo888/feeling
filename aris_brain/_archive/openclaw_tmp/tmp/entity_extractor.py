首先，用户要求输出纯 Python 代码，不要解释。所以，我需要提供一个完整的 EntityExtractor 类，包括所有必要的方法。

类接口已经给出：
class EntityExtractor:
    def extract(self, text: str) -> list[dict]:  # [{"text":"...", "type":"...", "confidence":0.9}]

但测试用例中没有提到 confidence，但接口中提到了 confidence:0.9。所以，我需要为每个提取的实体添加一个 confidence 字段。测试用例中没有明确 confidence 值，但接口指定了。我应该设置一个默认值，比如 0.9，或者根据匹配的确定性来设置。既然使用正则，我可以假设高置信度，比如 1.0 或 0.9。

从测试用例看：
- "读取test.py" -> [{"text":"test.py", "type":"file"}] 没有 confidence，但接口要求。所以，我需要包括它。
用户问题中的测试用例没有显示 confidence，但接口有。可能用户忘记在测试用例中写了。我应该遵循接口，包括 confidence。

接口中 confidence 是 0.9，所以我应该为每个匹配设置 confidence 为 0.9，或者可能基于匹配的质量。但既然是用正则，我可以固定为 1.0 或 0.9。为了简单，我设为 1.0。

现在，支持类型：file, url, command, time, number。

我需要为每种类型定义正则表达式。

1. file: 文件名如 test.py。可能匹配以 .py 结尾的单词，或者更一般的文件名。测试用例是 "test.py"，但可能还有其他扩展名。从上下文看，是提取文件名，所以可能匹配像 \w+\.\w+ 的模式，但更准确地说，文件名可能包含点。但为了简化，我可以匹配类似 \S+\.\S+ 的模式，但这样可能会匹配 URL 或其他东西。测试用例中 "读取test.py"，所以文件名是 "test.py"，没有路径。

正则：我可以用 r'(\S+\.\S+)' 来匹配文件名，但这不是唯一的。或者更精确地，对于文件名，假设它包含点且没有空格。在中文文本中，文件名可能被空格包围。

从测试用例：
- "读取test.py" -> 提取 "test.py"。所以，正则可以匹配 \w+\.\w+ 或类似。

为了通用，我定义正则：
- file: r'(\w+\.\w+)' 或 r'([^\s]+\.[^\s]+)' 但 URL 也有点，所以需要区分。

2. url: https://...。所以，正则可以是 r'(https?://\S+)' 匹配 http 或 https 链接。

3. command: 运行 ls -la 中的 ls。这里 command 是 "ls -la"，所以包括空格。测试用例是 "运行 ls -la"，提取 "ls -la"。所以，command 可能是一系列以空格分隔的单词，但以命令开头。在上下文中，可能用户输入像 "运行 ls -la"，所以 command 是 "ls -la"。但更一般地，command 可能是一个字符串，如 "ls -la" 或 "git commit"。

正则：我需要匹配 "运行" 后面的字符串，但类应该只提取实体，不考虑上下文。从测试用例，"运行 ls -la" 提取 "ls -la"，所以可能 command 是连续的单词，直到句尾或下一个实体。

但为了简单，我可以定义正则匹配类似 [a-zA-Z0-9]+(\s-[a-zA-Z0-9]+)* 或更通用。但测试用例中 command 是 "ls -la"，所以包括空格和连字符。

或许使用 r'([^\s]+(?:\s+[^\s]+)*)' 但这样太宽泛。

既然用户指定了类型，我可以为每种类型使用特定的正则模式。

4. time: 明天下午三点。所以，时间表达式可能像 "明天下午三点" 或 "上午10点"。正则可以匹配中文时间词。

5. number: 123。数字序列，如 \d+。

现在，接口要求返回 list of dict，每个 dict 有 "text"、"type" 和 "confidence"。

在 extract 方法中，我需要扫描文本，找到所有匹配的实体。

但实体可能有重叠，比如 URL 和文件名都可能有点。我需要确保正则不冲突。

或许按顺序匹配，先匹配更具体的模式。

测试用例中，每个输入只有一种实体类型，除了 "计算 123 乘以 456" 有两个数字。

所以，我需要支持多个匹配。

定义正则表达式：

- file: 假设文件名是像 test.py 这样的，所以 r'([a-zA-Z0-9_.]+\.py)' 但可能不止 .py。更一般：r'(\w+\.\w+)' 但可能匹配 URL 的域名部分。

URL 有 https://，所以优先匹配 URL。

- url: r'(https?://\S+)' 匹配 http:// 或 https:// 开头的字符串，直到空格或标点。

- command: 从测试用例，command 是 "ls -la"，所以可能匹配以字母开头，包含空格和连字符的字符串。但用户可能输入其他命令。

既然类是用于提取实体，我可以定义 command 为匹配类似 [a-zA-Z0-9]+(\s\-\w+)* 的模式，但更通用。

或许使用 r'([a-zA-Z0-9]+(?:\s\-[a-zA-Z0-9]+)*)' 但这样只匹配单个命令和选项。

在测试用例中，是 "ls -la"，所以两个部分。

为了简单，我假设 command 是连续的非中文字符，但中文文本中可能混合。

看测试用例文本："读取test.py" 是中文，"test.py" 是英文。"打开 https://example.com" 类似。"运行 ls -la" 中 "ls -la" 是英文。"明天下午三点" 是全中文。"计算 123 乘以 456" 中数字是英文数字。

所以，实体可能是英文或数字序列。

定义正则：

- number: r'(\d+)' 匹配一个或多个数字。

- time: 中文时间表达式，如 "明天下午三点"。可以定义模式如 r'((?:上午|下午|晚上)?\d{1,2}点(?:\d{1,2}分)?(?:\d{1,2}秒)?|明天|后天|昨天|今天)(?:上午|下午|晚上)?\d{1,2}点...' 但太复杂。

从测试用例，只有一个简单的时间字符串。我可以使用一个简单的正则匹配中文时间词，但为了通用，或许使用 r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?\d{1,2}点|\d{1,2}:\d{2})' 但测试用例是 "明天下午三点"。

或许使用一个宽泛的正则，匹配连续的中文字符，但可能误匹配。

既然用户指定了测试用例，我可以实现针对这些测试用例的正则，但要通用一些。

我决定为每种类型定义正则：

1. url: r'(https?://\S+)' 

2. file: r'(\w+\.\w+)' 但这样可能匹配 URL 中的部分，但 URL 已经优先匹配，所以先匹配 URL，然后 file。

顺序重要。我应该按优先级顺序定义正则，以避免冲突。

优先级：先匹配 URL，然后 command，然后 file，然后 time，然后 number？但 command 可能包含空格。

在测试用例中，"运行 ls -la" 提取 "ls -la"，所以 command 是完整的字符串。

定义 command 正则：假设 command 是以字母开头，后跟字母、数字、空格和连字符的字符串。使用 r'([a-zA-Z0-9]+(?:\s\-[a-zA-Z0-9]+)*)' 但 "ls -la" 匹配。

但更通用，command 可能包含更多字符。既然用户没有指定，我使用简单模式。

对于 time，使用一个正则匹配常见时间模式。例如 r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?\d{1,2}点(?:\d{1,2}分)?|(?:上午|下午|晚上)\d{1,2}点(?:\d{1,2}分)?|\d{1,2}:\d{2})' 但 "明天下午三点" 匹配。

测试用例是 "明天下午三点"，所以包含 "下午三点"。

在正则中，我可以写 r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?\d{1,2}点)' 来匹配如 "明天下午三点"。

但 "三点" 中的 "三" 是中文数字，所以需要处理中文数字。在测试用例中是 "三点"，所以数字是中文的。

所以，时间正则应该支持中文数字。例如 r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?[一二三四五六七八九十]+点)' 但 "三点" 匹配。

为了简化，我可以假设时间字符串是连续的中文字符，但可能太宽泛。

既然类只用于提取，我可以使用一个更简单的正则：对于 time，匹配包含 "点" 或 ":" 的字符串，但可能误匹配。

另一个想法：在文本中，实体可能被空格或标点分隔。我可以使用一个方法扫描文本，按顺序应用正则。

在 extract 方法中，我可以为每种类型定义正则，然后查找所有非重叠的匹配。

但不同类型的正则可能匹配重叠部分，所以我需要定义优先级，先匹配更具体的。

例如，URL 应该优先，因为它有 "://" 标志。

定义正则列表：

patterns = [
    (r'(https?://\S+)', 'url'),
    (r'([a-zA-Z0-9]+(?:\s\-[a-zA-Z0-9]+)*)', 'command'),  # 但可能匹配其他东西
    (r'(\w+\.\w+)', 'file'),
    (r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?\d{1,2}点)', 'time'),  # 简化，假设数字是阿拉伯数字，但测试用例是 "三点"，中文数字。
    (r'(\d+)', 'number'),
]

但 time 正则中，测试用例 "明天下午三点"，"三点" 中的 "三" 是中文数字，所以需要支持中文数字。

修改 time 正则：r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?[一二三四五六七八九十百千]+点)' 但可能匹配不准确。

为了通过测试用例，我可以使用更宽泛的正则：对于 time，匹配连续的中文字符，但可能误匹配 "明天" 本身。

从测试用例，"明天下午三点" 是一个整体，所以或许使用 r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?[一二三四五六七八九十\d]+点(?:\d+分)?)' 但复杂。

或许对于这个任务，只处理测试用例中的模式。

既然用户说 "用正则从中文文本中提取实体"，并且给出了测试用例，我可以实现一个基本版本。

我决定为 time 使用一个简单的正则：r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?\S+点\S*)' 但这样可能提取不准确。

看测试用例，只有 "明天下午三点"，所以可以匹配 "明天下午三点"。

我可以定义 time 正则为 r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?[一二三四五六七八九十\d]+点(?:\d+分)?)' 以匹配中文数字。

但 "三点" 匹配。

在 "明天下午三点" 中，是 "下午" 和 "三点"，所以正则应该捕获整个。

定义 time 正则：r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?[一二三四五六七八九十\d]+点(?:[一二三四五六七八九十\d]+分)?)'

对于 command，测试用例是 "ls -la"，所以使用 r'([a-zA-Z0-9]+(?:\s\-[a-zA-Z0-9]+)*)' 但 "ls -la" 匹配，因为 \s 匹配空格，\- 匹配连字符。

但 "ls -la" 中有一个空格，所以正则应该匹配空格。

在正则中，\s 匹配空格，所以 [a-zA-Z0-9]+ 匹配 "ls"，然后 \s 匹配空格，\- 匹配 "-"，[a-zA-Z0-9]+ 匹配 "la"。所以整体匹配 "ls -la"。

对于 file，测试用例是 "test.py"，所以使用 r'(\w+\.\w+)' 匹配 "test.py"，因为 \w 匹配字母、数字、下划线。

但 \w 在 Python 中匹配 [a-zA-Z0-9_]，所以 "test.py" 中的 "." 不被 \w 匹配，所以 \w+ 匹配 "test"，\. 匹配 "."，\w+ 匹配 "py"，所以整体匹配 "test.py"。

好。

对于 number，使用 r'(\d+)' 匹配数字序列。

现在，confidence 我设为 1.0 对于所有匹配，因为正则匹配是确定的。

在 extract 方法中，我需要遍历文本，使用 re.finditer 来查找所有匹配。

但不同类型的正则可能匹配重叠部分，所以我应该按顺序定义模式，并使用一个列表来记录已匹配的位置，以避免重叠。

更简单的方法是，为每种类型定义正则，然后查找所有匹配，然后如果匹配重叠，只保留第一个匹配或更具体的。

但为了简单，假设实体不重叠，或者使用一个方法按顺序匹配。

我可以在 extract 方法中，初始化一个空列表 results 和一个已匹配的集合 matched_indices。

然后对于每种模式，使用 re.finditer 查找所有匹配，对于每个匹配，检查是否与已匹配索引重叠，如果不重叠，则添加到 results。

定义 patterns 为一个列表，每个元素是 (regex, type)，按优先级排序。

优先级：先 url，然后 command，然后 file，然后 time，然后 number。因为 url 有特定前缀，command 可能包含空格，但 file 有点，可能冲突。

在测试用例中，没有重叠实体，所以可能没问题。

实现：

使用 re.finditer 获取匹配对象，对于每个匹配，获取 start 和 end 位置。

然后检查是否与任何已匹配的索引重叠。重叠定义为 [start, end) 区间与任何已添加的区间相交。

为了简化，我可以假设实体由空格分隔，但中文文本可能没有空格。

在测试用例中，"读取test.py" 没有空格在 "读取" 和 "test.py" 之间，但 "test.py" 是一个实体。

在正则匹配中，re.finditer 会找到非重叠的匹配，但不同模式可能匹配重叠部分。

所以，我需要一个方法来处理重叠。

一个简单的方法是：为每种类型应用正则，收集所有匹配，然后对于每个匹配，如果它的文本没有被包含在其他匹配中，则保留。但这不是顺序的。

既然用户要求输出纯代码，我可以实现一个基本版本，假设没有重叠，或者使用一个贪婪的方法。

我决定使用以下方法：

1. 定义模式列表，按优先级排序。

2. 使用 re.finditer 查找每个模式的匹配，对于每个匹配，如果它的 span 与任何之前添加的 span 不重叠，则添加到 results。

为了检查重叠，我可以维护一个列表 spans，每个元素是 (start, end)。

在添加新匹配前，检查新 span 是否与任何现有 span 重叠。重叠 if start < existing_end and end > existing_start。

定义函数 is_overlapping(span, existing_spans) 来检查。

在 extract 方法中：

results = []
matched_spans = []  # 列表 of (start, end)

for pattern, entity_type in patterns:
    for match in re.finditer(pattern, text):
        start, end = match.start(), match.end()
        text_match = match.group(1)  # 假设组1是匹配文本
        # 检查是否与任何现有 span 重叠
        overlapping = False
        for existing_start, existing_end in matched_spans:
            if start < existing_end and end > existing_start:
                overlapping = True
                break
        if not overlapping:
            results.append({
                "text": text_match,
                "type": entity_type,
                "confidence": 1.0
            })
            matched_spans.append((start, end))

但 patterns 中的正则应该有捕获组，group 1 是匹配文本。

在正则中，我定义了捕获组，所以 match.group(1) 是匹配文本。

现在，定义 patterns：

patterns = [
    (r'(https?://\S+)', 'url'),
    (r'([a-zA-Z0-9]+(?:\s\-[a-zA-Z0-9]+)*)', 'command'),
    (r'(\w+\.\w+)', 'file'),
    (r'((?:明天|今天|昨天|后天)(?:上午|下午|晚上)?[一二三四五六七八九十\d]+点(?:[一二三四五六七八