# ---------- 核心依赖（必须） ----------
numpy>=1.24,<3
aiohttp>=3.9.0,<4
pyyaml>=6.0,<7
python-dotenv>=1.0,<2
psutil>=5.9
requests>=2.28,<3

# ---------- MCP Server（可选，Hermes MCP 工具集成） ----------
mcp>=1.0,<2
